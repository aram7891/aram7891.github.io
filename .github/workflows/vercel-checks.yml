name: Vercel Deployment Checks

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Skip lint (static project)
        run: echo "No lint step — static project without Node.js root"

      - name: Set commit status
        uses: actions/create-commit-status@v1
        with:
          state: success
          description: "Vercel - andresramirez: lint"
          context: "lint"
          github_token: ${{ secrets.GITHUB_TOKEN }}
